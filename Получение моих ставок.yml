info:
  name: Получение моих ставок
  type: http
  seq: 11

http:
  method: GET
  url: "{{url}}api/bids/my"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
