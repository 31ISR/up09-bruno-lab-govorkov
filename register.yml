info:
  name: register
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}api/auth/register"
  body:
    type: json
    data: |-
      {
        "username": "pokoprime",
        "password": "123456",
        "email": "vlad@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
