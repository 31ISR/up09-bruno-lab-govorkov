info:
  name: Ставка на товар
  type: http
  seq: 10

http:
  method: POST
  url: api/items/:id/bids
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
