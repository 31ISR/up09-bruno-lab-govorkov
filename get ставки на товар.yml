info:
  name: get ставки на товар
  type: http
  seq: 4

http:
  method: GET
  url: "{{url}}api/items/:id/bids"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
